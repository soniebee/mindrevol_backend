# --- 1. LOCAL DATABASE (PostgreSQL Docker/Local) ---
spring.datasource.url=jdbc:postgresql://localhost:5432/mindrevol_db_dev
spring.datasource.username=postgres
spring.datasource.password=hothaovy

# --- 2. LOCAL REDIS ---
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.password=
spring.data.redis.database=0
# Local Redis thÆ°á»ng khÃ´ng báº­t SSL
spring.data.redis.ssl.enabled=false

# --- 3. DEBUGGING ---
# Báº­t log rÃµ rÃ ng Äá» sá»­a lá»i
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.com.mindrevol=DEBUG
logging.level.org.springframework.security=DEBUG

# --- 4. RELAXED SECURITY ---
# Cho phÃ©p frontend local gá»i vÃ o
app.cors.allowed-origins=http://localhost:5173,http://127.0.0.1:5173,https://localhost:5173

# --- 5. MINIO (Chá» dÃ¹ng Local) ---
minio.url=http://localhost:9000
minio.access-key=mindrevol_admin
minio.secret-key=mindrevol_password
minio.bucket-name=mindrevol-media

# --- 6. SSL / HTTPS (ÄÃ Táº®T Äá» TRÃNH Lá»I FILE KEYSTORE) ---
# Náº¿u báº¡n tháº­t sá»± cáº§n HTTPS local thÃ¬ má»i má» láº¡i, cÃ²n khÃ´ng cá»© Äá» tháº¿ nÃ y cho giá»ng Prod
# server.ssl.enabled=true
# server.ssl.key-store-type=PKCS12
# server.ssl.key-store=classpath:keystore.p12
# server.ssl.key-store-password=password
# server.ssl.key-alias=mindrevol

